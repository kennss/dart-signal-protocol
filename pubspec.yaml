name: dart_signal_protocol
description: Pure Dart implementation of the Signal Protocol (X3DH + Double Ratchet + Sender Key) with zero native dependencies.
version: 0.1.0-alpha.1
homepage: https://github.com/kennss/dart-signal-protocol
repository: https://github.com/kennss/dart-signal-protocol
issue_tracker: https://github.com/kennss/dart-signal-protocol/issues

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  pinenacl: ^0.6.0

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
